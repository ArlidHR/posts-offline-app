package com.github.arlidhr.posts_offline_app.components

/**
 * Reusable error message composable with retry action.
 * Usage: ErrorMessage(message, onRetry, modifier)
 * Displays error text with an optional retry button.
 */

